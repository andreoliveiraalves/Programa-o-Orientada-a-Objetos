function isPrime(N){
  if (Number.isInteger(N) == false || N<1){
      return false}
  else if (N==2){
      return true;}
  else {for(let i=2; i<N; i++){
      if(N%i==0){
          return false}
      else if (i == (N-1)){
          return true
          break}
   }
  }
}
